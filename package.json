{
  "name": "becp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cd frontend && bun dev",
    "build": "cd frontend && bun build",
    "test": "cd frontend && bun test",
    "contract:clean": "cd contracts && bunx hardhat clean",
    "contract:compile": "cd contracts && bunx hardhat compile",
    "contract:test": "cd contracts && bunx hardhat test",
    "contract:deploy:local": "cd contracts && bunx hardhat ignition deploy --network localhost",
    "contract:deploy:testnet": "cd contracts && bunx hardhat ignition deploy --network op-sepolia"
  },
  "workspaces": [
    "frontend",
    "contracts",
    "shared"
  ]
}
